name: Deploy

on:
  workflow_call:

concurrency: 
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  find-changes: 
    name: Find changed lists 🕵️
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4.1.7

      - name: Find changed lists
        id: find-changes
        run: |
          changed_lists=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep '^filters/' | cut -d/ -f2 | sort -u | jq -R -s -c 'split("\n") | map(select(. != ""))')
          echo "changed_lists=$changed_lists" >> $GITHUB_ENV
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    outputs:
      changed_lists: ${{ steps.find-changes.outputs.changed_lists }}

  deploy:
    name: Deploy 🚀
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: find-changes
    strategy:
      matrix:
        list: ${{ fromJson(env.changed_lists) }}
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4.1.7
      - run: corepack enable
      - uses: actions/setup-node@v4.0.3
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Build filters
        run: pnpm run build -- -l ${{ matrix.list }}

      - name: Dummy deploy
        run: cat out/${{ matrix.list }}.txt
